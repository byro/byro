# Documentation build (Zensical + mkdocstrings). Exact pins: Zensical is
# pre-1.0 and releases weekly, so every upgrade is a deliberate change.
# Regenerate after bumping a pin with, e.g.:
#   uv venv /tmp/zv && uv pip install --python /tmp/zv/bin/python \
#     "zensical==<version>" mkdocstrings mkdocstrings-python \
#   && uv pip freeze --python /tmp/zv/bin/python
#
# mkdocstrings depends on the mkdocs package itself (autorefs, ghp-import,
# mergedeep, watchdog, ...) even though Zensical does not use mkdocs; that
# transitive weight is accepted for the Python API reference (AP05). See
# docs/README.md.
click==8.5.0
deepmerge==3.0.1
ghp-import==2.1.0
griffelib==2.3.0
jinja2==3.1.6
markdown==3.10.3
markupsafe==3.0.3
mergedeep==1.3.4
mkdocs==1.6.1
mkdocs-autorefs==1.4.4
mkdocs-get-deps==0.2.2
mkdocstrings==1.0.6
mkdocstrings-python==2.0.9
packaging==26.3
pathspec==1.1.1
platformdirs==4.11.7
pygments==2.21.0
pymdown-extensions==11.0.2
python-dateutil==2.9.0.post0
pyyaml==6.0.3
pyyaml-env-tag==1.1
six==1.17.0
tomli==2.4.1
watchdog==6.0.0
zensical==0.0.59
